default:
  step_1:
    infra: { read_: settings/infra.yaml }
    service: { read_: settings/service.yaml }
    settings: { eval_: infra+service }
    docker:
      all: { read_: settings/docker/all.yaml }
      infra: { read_: settings/docker/infra.yaml }
      service: { read_: settings/docker/service.yaml }
  step_2:
    docker:
      infra: { eval_: docker.all+docker.infra }
      service: { eval_: docker.all+docker.service }