- set_fact:
    cw_watch_path: "{{(cw_watch_dir,cw_watch_pattern+'.txt')|join('/')}}"
- stat:
    path: "{{cw_watch_path}}"
  register: stat_result
- file:
    path: "{{cw_watch_path}}"
    state: touch
  when: not stat_result.stat.exists