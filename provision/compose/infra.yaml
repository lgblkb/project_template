default:
  definition:
    pre_: "${docker_infra}"

#    db: ${read:images/db.yaml}
#    rabbitmq: ${read:images/rabbitmq.yaml}
#    redis: ${read:images/redis.yaml}

development:
  definition:
    services:
      #    portainer: ${read:images/portainer.yaml}
      pgadmin: ${read:images/pgadmin.yaml}
